# ~/.tmuxinator/be.yml

name: be
root: ~/Code/vagrantfiles/shared/backend
startup_window: editor
windows:
  - editor: nvim
  - terminal: #empty
  - server:
      - git cull
      - vagrant ssh
      - cd backend
      - foreman start

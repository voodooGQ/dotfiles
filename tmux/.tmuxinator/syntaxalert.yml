# ~/.tmuxinator/syntaxalert.yml

name: syntaxalert
root: ~/Code/php/_wordpress/syntaxalert
startup_window: editor
windows:
  - editor: nvim
  - terminal: #empty
  - frontend:
      pre: nvm use v7.4.0
      root: ~/Code/php/_wordpress/syntaxalert/build
      panes:
        - gulp:
          - gulp
          - gulp watch
  - vagrant:
      root: ~/Code/php/_wordpress/syntaxalert/vagrant
      layout: tiled
      panes:
        - vagrant:
          - vagrant up
          - vagrant ssh
  - ssh:
      - ssh ssmith@syntaxalert.com


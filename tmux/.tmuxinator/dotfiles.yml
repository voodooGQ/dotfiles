# ~/.tmuxinator/dotfiles.yml

name: dotfiles
root: ~/dotfiles
startup_window: editor
windows:
  - editor: nvim
  - terminal:
      layout: tiled
      panes:
        - git: git status
        - cli: clear
